Good morning QUIZ!
좋은 하루 시작입니다. ^^

## 📝 JavaScript 핵심 기능 객관식 문제 (20문항)

**1. (함수) 다음 `login` 함수 호출의 결과로 변수 `result`에 할당되는 값은 무엇입니까?**

```javascript
function login(id, pwd){
    let result = id == 'himan' && pwd =='7777' ?"로그인 성공" : "로그인 실패"
    return result;
}
let result = login('himan', '8888');
```

1)  "로그인 성공"
#2)  "로그인 실패"
3)  true
4)  false

**2. (함수) 다음 `buy` 함수를 호출한 결과로 변수 `tot`에 할당되는 값은 무엇입니까?**

```javascript
function buy(price, cnt){
    return price*cnt
}
let tot = buy(3000, 2);
```

1)  3000
2)  2
#3)  6000
4)  "30002"

**3. (내장 함수) `isNaN()` 함수에 대한 설명으로 틀린 것은 무엇입니까?**

1)  "Is Not a Number?"의 약자입니다.
2)  주어진 값이 숫자인지 아닌지 검사합니다.
#3)  `isNaN(123)`의 결과는 `true`입니다. NaN되면 true  숫자가 아니면 
4)  `isNaN(parseInt("안녕"))`의 결과는 `true`입니다.

**4. (내장 함수) 다음 코드의 실행 결과로 `prtStr`에 추가되는 내용은 무엇입니까?**

```javascript
let input1 = parseInt("123");
let prtStr = `<h3>123은 ${ isNaN(input1)?"숫자가 아닙니다":"숫자입니다." }</h3>`;
```

1)  `<h3>123은 숫자가 아닙니다</h3>`
#2)  `<h3>123은 숫자입니다.</h3>`
3)  `<h3>123은 NaN입니다</h3>`
4)  `<h3>123은 123입니다</h3>`

**5. (형변환) `parseInt()` 함수에 대한 설명으로 틀린 것은 무엇입니까?**

1)  문자열에서 정수만 추출합니다.
2)  `parseInt("120px")`의 결과는 120입니다.
#3)  `parseInt("95.5점")`의 결과는 95.5입니다.  정수까지만 추출..
4)  `parseInt("$120")`의 결과는 `NaN`입니다.  숫자로 시작하여 문자가 나오기 전까지 추출..

**6. (형변환) 다음 중 실행 결과가 `NaN` (Not a Number)인 것은 무엇입니까?**

1)  `parseInt("75점")`
2)  `parseFloat("95.7점")`
3)  `Number("3000")`
#4)  `Number("20%")`  Number() 실수/정수 모두 추출할 수 있지만 문자가 추가되면 NaN 발생..

**7. (배열과 반복문) 다음 코드에서 `partyMembers.length`의 값은 무엇입니까?**

```javascript
let partyMembers = ['피카츄','파이리','꼬부기'];
```

1)  0
2)  2
#3)  3
4)  4

**8. (배열과 반복문) `studyGroup` 배열을 순회하는 `for` 문입니다. `i`가 가질 수 있는 마지막 값은 무엇입니까?**

```javascript
let studyGroup = ['영희','철수','민수','지아','준호'];
for(let i=0; i < studyGroup.length; i++){
    // ...
}
```

1)  3
2)  4
#3)  5  i값이 5까지되고 반복처리는 하지 못 한다.
4)  6

**9. (객체) `person` 객체의 `age` 값에 접근하는 올바른 방법은 무엇입니까?**

```javascript
let person = {name:"김철수", age:25, isStudent:true};
```

1)  `person[age]`
2)  `person("age")`
#3)  `person.age`
4)  `person.getAge()`

**10. (객체) `product` 객체의 `show` 메서드를 호출하는 올바른 방법은 무엇입니까?**
let arry = [3000,4000,5000]
arry[0]
let product = {name:"사과", price:3000, 
               show:function(){ 
               		alert(`물건명:${this.name}, 가격:${this.price}`) }}
product.name ==> "사과"
객체(저장공간 - 여러개 유형의 속성:속성값)
product.show() ==> 객체에 종속된 선언된 함수를 수행할 때..
속성 : 속성값 
price : 3000   price에 속성값을 3000 하듯이
show  : function(){  }
let no = 10;
객체에 들어가기전에 함수..
// 변수형 함수를 할당
let call = function(){

}
// 선언적 함수
function call(){

}

call() // 선언된 함수 실행 () => 반드시 붙음..




```javascript
let product = {name:"사과", price:3000, cnt:5,
                show:function(){ alert(`${this.name}`); }
              };
```

1)  `product.show`
#2)  `product.show()`
3)  `show(product)`
4)  `product["show"]`

**11. (JSON) JavaScript 객체와 JSON의 차이점에 대한 설명으로 올바른 것은 무엇입니까?**

1)  JSON은 키(key)에 따옴표를 사용하지 않아도 됩니다. 키 큰따옴표 사용..
2)  JavaScript 객체는 항상 전체가 하나의 문자열로 취급됩니다. ==> 이기종에 기기에 전송 가능..
#3)  JSON은 데이터 교환을 위한 형식이며, 키(key)를 반드시 **큰따옴표**(`""`)로 감싸야 합니다.
4)  JSON은 함수(메서드)를 포함할 수 있지만, JavaScript 객체는 불가능합니다.

**12. (JSON) JavaScript 객체를 JSON 문자열로 변환하는 함수(여권 발급)는 무엇입니까?**

1)  `JSON.parse()`
#2)  `JSON.stringify()`
	let person = {name:'홍길동'}  // javascript object
	let personStr = JSON.stringify(person)  // `{"name":"홍길동"}`
	==> 전송(어디로나 갈수있는 여권발급) ==> 다른 기종(컴퓨터,핸드폰...)
	==> 
	let personOb = JSON.parse(personStr)  // 문자열 ==> 객체
	==> AJAX (javascript <==> java) 		
3)  `Number()`
4)  `toString()`

**13. (객체형 배열) `aptBuilding` 배열에서 "이영희"라는 값에 접근하는 방법은 무엇입니까?**

```javascript
const aptBuilding = [
    {room:101, owner:"김철수"},
    {room:102, owner:"이영희"},
    {room:103, owner:"박지성"},
];
aptBuilding[0] aptBuilding[0].room  aptBuilding[0].owner
aptBuilding[1] aptBuilding[1].room  aptBuilding[1].owner
aptBuilding[2] aptBuilding[2].room  aptBuilding[2].owner

```

1)  `aptBuilding[0].owner`
#2)  `aptBuilding[1].owner`
3)  `aptBuilding[1][1]`
4)  `aptBuilding.owner[1]`

**14. (객체형 배열) 다음 `empList` 배열의 `length` 값은 얼마입니까?**

```javascript
let empList = [
    {empno:7789, ename:"홍길동",sal:3500 },
    {empno:7790, ename:"김길동",sal:4500 },
    {empno:7791, ename:"신길동",sal:5500 }
];
```

1)  2
#2)  3
3)  4
4)  9

**15. (String) 다음 코드의 실행 결과로 `trimId`에 할당되는 값은 무엇입니까?**

```javascript
let messyId = "   SuperMan123   ";
let trimId = messyId.trim();
```

1)  `"   SuperMan123   "`
#2)  `"SuperMan123"`
3)  `"SuperMan"`
4)  `undefined`

**16. (String) 다음 코드의 실행 결과로 `tagArry[1]`의 값은 무엇입니까?**

```javascript
let tagString = "자바스크립트, 프로그래밍, 코딩, KDT";
let tagArry = tagString.split(',');
```

1)  `"자바스크립트"`
#2)  `" 프로그래밍"`
3)  `"코딩"`
4)  `undefined`

**17. (String) 다음 코드의 실행 결과로 `prefix`에 할당되는 값은 무엇입니까?**

```javascript
let filename = "MyReport.PDF";
let prefix = filename.slice(0, 8);
```

#1)  `"MyReport"`
2)  `"MyReport."`
3)  `".PDF"`
4)  `"MyRepor"`

**18. (Array) 배열의 맨 뒤에 새로운 요소를 추가하는 메서드는 무엇입니까?**

1)  `pop()`
#2)  `push()`
3)  `slice()`
4)  `split()`

**19. (Array) 다음 코드의 실행 결과로 `prtStr`에 추가되는 내용은 무엇입니까?**

```javascript
let cart = ['사과', '바나나'];
cart.push("오렌지");
let prtStr = `<h3>${cart.pop()}</h3>`;  cart ['사과', '바나나']
```

1)  `<h3>사과</h3>`
2)  `<h3>바나나</h3>`
#3)  `<h3>오렌지</h3>`
4)  `<h3></h3>`

**20. (Array) `forEach`를 사용한 반복문입니다. 빈칸에 들어갈 올바른 매개변수 순서는 무엇입니까?**

```javascript
let prices = [3000, 4000, 5000];
prices.forEach( function( price , idx ){  단위데이터변수, index번호
    idx 숫자형
    price 단위데이터 
});
```
for(let idx = 0;idx<prices.length;idx++){
	idx : 숫자형 0 1 2
	prices[idx]
}
배열에서 for문 처리..
for(let idx in prices){
	idx : 문자열형 '0','1','2'
	prices[idx]  prices['0'] prices['1']  prices['2']
}
객체에서 for문 처리
let person = {name:"홍길동",age:25,loc:"수원"}
for(let pro in person){
	pro : 문자열 'name'  'age'  'loc'
	person[pro] :  person['name']  person.name
	               person['age']   person.age
	               person['loc']   person.loc
}




1)  index, price
#2)  price, index
3)  key, value
4)  object, index

-----

## 👨‍💻 JavaScript 핵심 기능 주관식 문제 (5문항)

**문제 1. (함수 정의)**
물건 가격(`price`)과 개수(`cnt`)를 매개변수로 받아, 총계(가격 \* 개수)를 
\*\*반환(return)\*\*하는 함수 `calculateTotal`을 작성하세요.
function calculateTotal(price, cnt){
	return price*cnt;
}
let tot = calculateTotal(3000,2)  6000

**문제 2. (형변환 비교)**
`parseInt("10.7점")`과 `parseFloat("10.7점")`의 실행 결과가 각각 무엇인지, 
	그리고 왜 그렇게 다른 결과가 나오는지 설명하세요.
 parseInt("10.7점") 10	
 parseFloat("10.7점") 10.7
 
**문제 3. (객체 생성)**
다음 정보를 포함하는 `book`이라는 이름의 JavaScript **객체**를 생성하는 코드를 작성하세요.

  * 키: `title`, 값: "자바스크립트 입문"
  * 키: `price`, 값: 30000
let book = {
	title:"자바스크립트입문",
	price:30000
}  

  

**문제 4. (객체형 배열 접근)**
아래 `empList` 배열에서, 두 번째 사원(김길동)의 \*\*급여(sal)\*\*에 접근하는 코드를 
  한 줄로 작성하세요.

```javascript
let empList = [
    {empno:7789, ename:"홍길동",sal:3500 },
    {empno:7790, ename:"김길동",sal:4500 },
    {empno:7791, ename:"신길동",sal:5500 }
];
``` empList[1].sal
empList.forEach(function(emp, idx){
	emp =  {empno:7789, ename:"홍길동",sal:3500 }
	emp =  {empno:7790, ename:"김길동",sal:4500 }
	emp =  {empno:7791, ename:"신길동",sal:5500 }
	htmlStr =`<tr>
	              <td>${emp.empno}</td>
	              <td>${emp.ename}</td>
	              <td>${emp.sal}</td>
			  </tr>`
})
**문제 5. (String/Array 메서드)**
아래 `colorStr` 문자열을 `"-"` 기호를 기준으로 **분리하여 배열로** 만든 후, 
그 배열의 **맨 뒤**에 "초록"을 **추가**하는 코드를 두 줄로 작성하세요.
```javascript
let colorStr = "빨강-파랑-노랑";
let colorArr = colorStr.split("-")  ["빨강","파랑","노랑"]
colorArr.push("초록")  ["빨강","파랑","노랑","초록"]
```

